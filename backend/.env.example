# LLM Provider Configuration
# Supported providers: 'openai' or 'gemini'
LLM_PROVIDER=openai

# OpenAI Configuration (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview

# Gemini Configuration (required if LLM_PROVIDER=gemini)
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-2.5-flash

# GitHub Configuration (OPTIONAL - not needed with OAuth authentication)
# Only required if you want guest users to have higher rate limits for public PRs
# GITHUB_TOKEN=your_github_token_here

# GitHub OAuth Configuration (required for authentication)
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_CALLBACK_URL=http://localhost:4000/api/auth/github/callback

# JWT & Session Secrets (generate random strings, min 32 characters)
JWT_SECRET=your_jwt_secret_here_min_32_chars_random_string
SESSION_SECRET=your_session_secret_here_min_32_chars_random_string

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:5173

# Server Configuration
PORT=4000
NODE_ENV=development
